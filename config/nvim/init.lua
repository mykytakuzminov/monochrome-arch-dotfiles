-- ==================================================
-- Neovim Main Configuration
-- File: init.lua
-- Purpose: Entry point for Neovim configuration
--          Loads lazy.nvim plugin manager and triggers plugin setup
-- ==================================================

-- Load lazy.nvim configuration
require("config.lazy")
